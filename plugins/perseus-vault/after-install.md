# Perseus Vault memory provider installed ✓

Next steps:

1. Get a bearer token for your Perseus Vault MCP endpoint.
2. Run `hermes memory setup` and pick **perseus-vault**
   (it will prompt for the token and store it in `.env`).
3. Start a new session — `hermes memory status` should show
   `perseus-vault ← active`, `available ✓`.

Docs: https://github.com/Perseus-Computing-LLC/hermes-plugin-perseus-vault
